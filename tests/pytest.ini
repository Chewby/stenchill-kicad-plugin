[pytest]
# This file pins pytest's rootdir to plugin/tests, so collection never walks up
# into the `plugin` package (whose __init__.py imports pcbnew + registers the
# plugin — KiCad-only).
#
# Run the suite with `pytest` from THIS directory, or `pytest tests` from
# plugin/. A bare `pytest` from plugin/ cannot work: rootdir would then be the
# package dir and pytest would import __init__.py -> pcbnew and abort.
addopts =
