[pytest]
# Presence of this file pins pytest's rootdir to plugin/tests when invoked from
# here, so collection never walks up into the `plugin` package (whose
# __init__.py imports pcbnew + registers the plugin — KiCad-only).
addopts =
